# Obraz bazowy runtime per architektura. python:3.12-slim jest multi-arch
# (jeden tag → właściwy wariant aarch64/amd64), więc ta sama wartość.
build_from:
  aarch64: python:3.12-slim
  amd64: python:3.12-slim
